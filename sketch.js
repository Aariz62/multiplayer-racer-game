var database
var gameState=0
var playerCount

function setup(){
  var canvas=createCanvas(displayWidth,displayHeight);

  database=firebase.database()

  form=new Form()

 form.display()


}
function draw()
{
  background("white");
  
  
}
